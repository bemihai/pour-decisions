// Module declaration for plotly.js-dist-min (minified Plotly bundle).
// The package ships a pre-built .js file with no bundled TypeScript types;
// we use the full plotly.js types (@types/plotly.js) for the type interface
// and import the dist bundle only at runtime inside PlotlyChart.tsx.
declare module "plotly.js-dist-min";

