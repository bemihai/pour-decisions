"""Pour Decisions FastAPI backend API.

Provides REST endpoints wrapping the existing repository layer, agent invocation,
and retrieval pipeline. Replaces the Streamlit UI's direct access to business logic
with a clean API boundary.
"""

